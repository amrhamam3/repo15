<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_dark">

    <!-- Top Bar -->
    <LinearLayout android:id="@+id/topBar"
        android:layout_width="0dp" android:layout_height="wrap_content"
        android:orientation="horizontal" android:gravity="center_vertical"
        android:background="@color/surface_dark"
        android:paddingStart="12dp" android:paddingEnd="8dp"
        android:paddingTop="10dp" android:paddingBottom="10dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView android:id="@+id/appLogo"
            android:layout_width="32dp" android:layout_height="32dp"
            android:layout_marginEnd="8dp"
            android:src="@drawable/app_logo"
            android:contentDescription="@string/app_name" />

        <TextView android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1" android:text="@string/app_name"
            android:textColor="@color/text_primary" android:textSize="17sp" android:textStyle="bold" />

        <ImageButton android:id="@+id/btnWhatsapp"
            android:layout_width="38dp" android:layout_height="38dp"
            android:layout_marginEnd="6dp"
            android:background="@android:color/transparent"
            android:src="@drawable/ic_whatsapp"
            android:contentDescription="واتساب" android:padding="4dp" />

        <Button android:id="@+id/btnOpenFile"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="@string/open_file" android:textAllCaps="false"
            android:textSize="13sp" android:minWidth="0dp"
            android:paddingStart="12dp" android:paddingEnd="12dp"
            android:backgroundTint="@color/accent_orange" />
    </LinearLayout>

    <!-- GL Viewer -->
    <FrameLayout android:id="@+id/viewerContainer"
        android:layout_width="0dp" android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/topBar"
        app:layout_constraintBottom_toTopOf="@id/displayToolbar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.amr3d.preview.pro.GLViewerView
            android:id="@+id/glViewerView"
            android:layout_width="match_parent" android:layout_height="match_parent" />

        <TextView android:id="@+id/emptyStateText"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/no_file_loaded"
            android:textColor="@color/text_secondary" android:textSize="16sp" />

        <!-- View Cube -->
        <LinearLayout android:id="@+id/viewCubeContainer"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:layout_gravity="top|end" android:layout_margin="8dp"
            android:orientation="vertical" android:gravity="center_horizontal">

            <com.amr3d.preview.pro.ViewCubeView
                android:id="@+id/viewCube"
                android:layout_width="80dp" android:layout_height="80dp" />

            <LinearLayout android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:orientation="horizontal" android:layout_marginTop="4dp">

                <Button android:id="@+id/btnViewBack"
                    android:layout_width="34dp" android:layout_height="22dp"
                    android:text="B" android:textSize="8sp"
                    android:textColor="@color/text_primary"
                    android:padding="0dp" android:minWidth="0dp" android:minHeight="0dp"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton" />

                <Button android:id="@+id/btnViewLeft"
                    android:layout_width="34dp" android:layout_height="22dp"
                    android:text="L" android:textSize="8sp"
                    android:textColor="@color/text_primary"
                    android:padding="0dp" android:minWidth="0dp" android:minHeight="0dp"
                    android:layout_marginStart="2dp"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton" />

                <Button android:id="@+id/btnViewBottom"
                    android:layout_width="34dp" android:layout_height="22dp"
                    android:text="Bot" android:textSize="7sp"
                    android:textColor="@color/text_primary"
                    android:padding="0dp" android:minWidth="0dp" android:minHeight="0dp"
                    android:layout_marginStart="2dp"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton" />
            </LinearLayout>
        </LinearLayout>

        <!-- Measurement Card -->
        <androidx.cardview.widget.CardView android:id="@+id/measurementCard"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:layout_gravity="top|start" android:layout_margin="12dp"
            app:cardBackgroundColor="@color/surface_dark"
            app:cardCornerRadius="10dp" app:cardElevation="6dp"
            android:visibility="gone">
            <TextView android:id="@+id/measurementText"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:padding="12dp" android:textColor="@color/accent_blue"
                android:textSize="14sp" android:fontFamily="monospace" />
        </androidx.cardview.widget.CardView>

        <!-- Inspection Card -->
        <androidx.cardview.widget.CardView android:id="@+id/inspectionCard"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:layout_gravity="bottom" android:layout_margin="12dp"
            app:cardBackgroundColor="@color/surface_dark"
            app:cardCornerRadius="10dp" app:cardElevation="6dp"
            android:visibility="gone">
            <TextView android:id="@+id/inspectionText"
                android:layout_width="match_parent" android:layout_height="wrap_content"
                android:padding="14dp" android:textColor="@color/text_primary"
                android:textSize="13sp" android:fontFamily="monospace"
                android:lineSpacingExtra="4dp" />
        </androidx.cardview.widget.CardView>

    </FrameLayout>

    <!-- Display Toolbar -->
    <LinearLayout android:id="@+id/displayToolbar"
        android:layout_width="0dp" android:layout_height="wrap_content"
        android:orientation="horizontal" android:background="@color/surface_dark"
        android:paddingTop="6dp" android:paddingBottom="6dp"
        android:paddingStart="8dp" android:paddingEnd="8dp" android:gravity="center"
        app:layout_constraintBottom_toTopOf="@id/bottomToolbar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ToggleButton android:id="@+id/btnWireframe"
            android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1" android:layout_marginEnd="4dp"
            android:textOn="@string/wireframe" android:textOff="@string/wireframe"
            android:textSize="12sp" android:textColor="@color/text_primary"
            android:background="@drawable/bg_toggle_button" />

        <Button android:id="@+id/btnMaterial"
            android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1" android:layout_marginEnd="4dp"
            android:text="المادة" android:textAllCaps="false"
            android:textSize="12sp" android:textColor="@color/text_primary"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton" />

        <Button android:id="@+id/btnUnit"
            android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1" android:layout_marginEnd="4dp"
            android:text="@string/unit_mm" android:textAllCaps="false"
            android:textSize="12sp" android:textColor="@color/text_primary"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton" />

        <Button android:id="@+id/btnExport"
            android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/export_image" android:textAllCaps="false"
            android:textSize="12sp" android:textColor="@color/text_primary"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton" />
    </LinearLayout>

    <!-- Bottom Toolbar -->
    <LinearLayout android:id="@+id/bottomToolbar"
        android:layout_width="0dp" android:layout_height="wrap_content"
        android:orientation="horizontal" android:background="@color/surface_dark"
        android:paddingTop="8dp" android:paddingBottom="8dp" android:gravity="center"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ToggleButton android:id="@+id/btnMeasureTool"
            android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1" android:layout_marginStart="8dp" android:layout_marginEnd="4dp"
            android:textOn="@string/measure_tool" android:textOff="@string/measure_tool"
            android:textColor="@color/text_primary"
            android:background="@drawable/bg_toggle_button" />

        <Button android:id="@+id/btnInspect"
            android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1" android:layout_marginEnd="4dp"
            android:text="@string/inspect_tool" android:textAllCaps="false"
            android:textColor="@color/text_primary"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton" />

        <Button android:id="@+id/btnResetView"
            android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1" android:layout_marginEnd="8dp"
            android:text="@string/reset_view" android:textAllCaps="false"
            android:textColor="@color/text_primary"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
